rules_version = '2';

// TEMPORARY COMPLETELY OPEN RULES FOR TESTING - DO NOT USE IN PRODUCTION
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORARY: Allow all read/write access for testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
